
{
  "visible_filenames": [
    "cyber-dojo.sh",
    "instructions",
    "Game.cs",
    "GameRunner.cs",
    "GameRunnerTest.cs"
  ],

  "filename_extension" : ".cs",

  "display_name": "Ugly Trivia, C# original",

  "image_name": "cyberdojofoundation/csharp_moq",

  "red_amber_green" : [
    "lambda { |output|",
    "  return :red   if /^Errors and Failures:/.match(output)",
    "  return :green if /^Tests run: (\\d*), Errors: 0, Failures: 0/.match(output)",
    "  return :amber",
    "}"
  ]
}
