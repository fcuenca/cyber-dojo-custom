{
  "visible_filenames": [
    "RouletteRunner.java",
    "RouletteTests.java",
    "instructions",
    "cyber-dojo.sh"
  ],

  "filename_extension" : ".java",

  "display_name": "Roulette Wheel, Java Mockito",

  "image_name": "cyberdojofoundation/java_mockito",

  "red_amber_green" : [
    "lambda { |output|",
    "  return :red   if /^Tests run: (\\d*),(\\s)+Failures: (\\d*)/.match(output)",
    "  return :green if /^OK \\((\\d*) test/.match(output)",
    "  return :amber",
    "}"
  ]
}
