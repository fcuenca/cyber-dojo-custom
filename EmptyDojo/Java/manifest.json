{
  "visible_filenames": [
    "TestSuite.java",
    "instructions",
    "cyber-dojo.sh"
  ],

  "filename_extension" : ".java",

  "display_name": "Empty Dojo, Java Practice",

  "image_name": "cyberdojofoundation/java_junit",

  "red_amber_green" : [
    "lambda { |output|",
    "  return :red   if /^Tests run: (\\d*),(\\s)+Failures: (\\d*)/.match(output)",
    "  return :green if /^OK \\((\\d*) test/.match(output)",
    "  return :amber",
    "}"
  ]
}
